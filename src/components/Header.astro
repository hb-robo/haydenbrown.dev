---
import HeaderLink from './HeaderLink.astro';
import { SITE_TITLE } from '../consts';
import * as Icon from '@astropub/icons';
---

<header>
  <nav>
    <a class="site-title" href="/">
      <svg viewBox="0 0 16 16" aria-hidden="true" width="2em" height="2em">
        <rect width="100%" height="100%" fill="#FFD700"></rect>
        <Icon.Code fill="black" />
      </svg>
      <!-- <h2>{SITE_TITLE}</h2> -->
    </a>
    <div class="internal-links">
      <HeaderLink href="/about">about</HeaderLink>
      <HeaderLink href="/cv">cv</HeaderLink>
      <HeaderLink href="/blog">blog</HeaderLink>
    </div>
  </nav>
</header>
<style>
  header {
    margin: 0;
    padding: 0 1em;
    background: white;
    box-shadow: 0 2px 8px rgba(var(--black), 5%);
  }
  nav {
    margin: 0 auto;
    display: flex;
    align-items: center;
    justify-content: space-between;
    max-width: calc(100% - 2em);
    width: 720px;
    padding: 1em;
  }
  .site-title {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    text-decoration: none;
    color: var(--black);
  }
  .site-title h2 {
    margin: 0;
    font-size: 1em;
  }
  .internal-links {
    display: flex;
    gap: 1em;
  }

  nav a {
    color: var(--black);
    border-bottom: 4px solid transparent;
    text-decoration: none;
  }
  nav a.active {
    text-decoration: none;
    border-bottom-color: var(--accent);
  }
</style>
